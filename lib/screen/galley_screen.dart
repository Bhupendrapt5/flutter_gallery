import 'package:flutter/material.dart';

class GalleryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // print(DateTime.now()..toString().replaceAll(new RegExp(r'[-.:\s]'), ''));
    return Container(
      
    );
  }
}